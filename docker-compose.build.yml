services:
  builder:
    build:
      context: .
      dockerfile: Dockerfile.build
      target: builder
    image: pipepipe-builder:latest
    command: ["bash", "-c", "cp /app/PipePipeClient/app/build/outputs/apk/debug/*.apk /outputs/ && ls -la /outputs/"]
    volumes:
      - ./outputs:/outputs
